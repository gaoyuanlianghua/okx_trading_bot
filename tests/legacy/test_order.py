import time\nimport schedule\nfrom core.api.okx_rest_client import OkxRestClient\n\ndef place_and_cancel_order():\n    client = OkxRestClient()\n    # Place a test order\n    order_id = client.place_order(symbol='BTC-USDT', side='buy', price='20000', size='0.001')\n    print(f'Placed order: {order_id}')\n    # Cancel the order after 1 minute\n    time.sleep(60)\n    client.cancel_order(order_id=order_id)\n    print(f'Cancelled order: {order_id}')\n\n# Schedule the task\nschedule.every(3).minutes.do(place_and_cancel_order)\n\nwhile True:\n    schedule.run_pending()\n    time.sleep(1)
