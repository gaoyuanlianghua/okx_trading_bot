项目要根据ok api 指南.md进行规范，在每次进行代码调整时都要进行规范调整，全程在SSH上运行
API 管理器需要进行每次任务必要核对的工具，需要在任务完成对它的不足优化
API 响应解析器进行每次任务必要核对的工具，需要在任务完成对它的不足优化
API 客户端 智能体系统 事件总线都是进行每次任务必要核对的任务，需要在任务完成对它的不足优化
以后所有的脚本必须依附在main_new.py下，不能脱离主程序，不能开起另外编辑除main_new.py以外的单独运行程序
